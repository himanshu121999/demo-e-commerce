import { ToastContainer } from "react-toastify";
import "react-toastify/dist/ReactToastify.css";

const Notification: React.FC = () => <ToastContainer position="top-right" />;

export default Notification;
